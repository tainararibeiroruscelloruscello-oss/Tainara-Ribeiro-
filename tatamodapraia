# Tainara-Ribeiro-<!doctype html>

<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Água & Sol — Moda Praia</title>
  <meta name="description" content="Loja de moda praia responsiva — bikinis, maiôs e acessórios." />
  <style>
    :root{
      --accent:#FF7A59;
      --accent-2:#FFB08A;
      --bg:#fff;
      --muted:#6b7280;
      --card:#ffffff;
      --glass: rgba(255,255,255,0.6);
      --shadow: 0 8px 24px rgba(16,24,40,0.08);
      font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    }
    *{box-sizing:border-box}
    body{margin:0;background:linear-gradient(180deg,#E8F8FF 0%, #FFF 60%);color:#111}
    header{display:flex;align-items:center;justify-content:space-between;padding:18px 28px;background:transparent;position:sticky;top:0;backdrop-filter: blur(6px);z-index:40}
    .logo{display:flex;align-items:center;gap:10px;font-weight:700}
    .logo .mark{width:40px;height:40px;border-radius:50%;background:linear-gradient(135deg,var(--accent),var(--accent-2));display:flex;align-items:center;justify-content:center;color:white;font-size:18px}
    nav{display:flex;gap:14px;align-items:center}
    nav a{color:#111;text-decoration:none;padding:8px;border-radius:8px}
    nav a.cta{background:var(--accent);color:white;padding:10px 14px}/* Hero */
.hero{display:grid;grid-template-columns:1fr 420px;gap:28px;align-items:center;padding:48px 6vw}
.hero-left h1{font-size:42px;line-height:1.02;margin:0}
.hero-left p{color:var(--muted);margin-top:12px}
.btn{display:inline-block;padding:12px 18px;border-radius:12px;background:var(--accent);color:white;text-decoration:none}

/* Filters + grid */
.controls{display:flex;gap:12px;flex-wrap:wrap;align-items:center;padding:22px 6vw}
.search{flex:1;min-width:180px}
.search input{width:100%;padding:12px;border-radius:10px;border:1px solid #e6edf3}
.grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:18px;padding:0 6vw 60px}
.card{background:var(--card);border-radius:14px;overflow:hidden;box-shadow:var(--shadow);display:flex;flex-direction:column}
.card img{width:100%;height:220px;object-fit:cover}
.card .info{padding:12px}
.price{font-weight:700}
.muted{color:var(--muted);font-size:14px}
.tags{display:flex;gap:8px;margin-top:8px;flex-wrap:wrap}
.tag{background:#f3f7fb;padding:6px 8px;border-radius:999px;font-size:12px}

/* Modal / Cart */
.modal-backdrop{position:fixed;inset:0;background:rgba(0,0,0,0.35);display:none;align-items:center;justify-content:center}
.modal{background:white;border-radius:12px;max-width:720px;width:94%;box-shadow:0 20px 60px rgba(2,6,23,0.4);overflow:hidden}
.modal .content{display:flex;gap:18px;padding:18px}
.cart-btn{position:relative}
.cart-count{position:absolute;top:-6px;right:-6px;background:var(--accent);color:white;border-radius:50%;padding:4px 7px;font-size:12px}

/* Footer */
footer{padding:28px 6vw;background:linear-gradient(180deg,transparent,rgba(255,250,248,0.4));display:flex;justify-content:space-between;align-items:center}

/* Responsive */
@media (max-width:900px){
  .hero{grid-template-columns:1fr;gap:18px;text-align:center}
  .hero-right{order:-1}
}

  </style>
</head>
<body>
  <header>
    <div class="logo" aria-label="Água & Sol">
      <div class="mark">AS</div>
      <div>
        <div>Água & Sol</div>
        <div style="font-size:12px;color:var(--muted)">Moda Praia</div>
      </div>
    </div>
    <nav>
      <a href="#produtos">Produtos</a>
      <a href="#sobre">Sobre</a>
      <a class="cta" id="exploreBtn">Explorar</a>
      <button class="cart-btn" id="openCart" aria-label="Abrir carrinho">👜 <span class="cart-count" id="cartCount">0</span></button>
    </nav>
  </header>  <main>
    <section class="hero">
      <div class="hero-left">
        <h1>Moda praia leve, colorida e feita pra curtir o sol.</h1>
        <p>Maiôs, biquínis e acessórios pensados para conforto e estilo. Peças com tecidos de secagem rápida e modelagens que valorizam cada corpo.</p>
        <div style="margin-top:18px">
          <a class="btn" id="shopAll">Ver coleções</a>
        </div>
      </div>
      <div class="hero-right">
        <img src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?auto=format&fit=crop&w=800&q=60" alt="praia" style="width:100%;border-radius:14px;box-shadow:var(--shadow);">
      </div>
    </section><section class="controls" aria-label="Controles de busca">
  <div class="search">
    <input type="search" id="searchInput" placeholder="Procure: biquíni, maiô, canga..." />
  </div>
  <div>
    <select id="filterCategory">
      <option value="all">Todas</option>
      <option value="bikini">Biquíni</option>
      <option value="maio">Maiô</option>
      <option value="acessorio">Acessório</option>
    </select>
  </div>
  <div>
    <select id="sortBy">
      <option value="popular">Mais populares</option>
      <option value="price-asc">Preço: baixo → alto</option>
      <option value="price-desc">Preço: alto → baixo</option>
    </select>
  </div>
</section>

<section id="produtos" class="grid" aria-live="polite">
  <!-- Cards serão inseridos pelo JS -->
</section>

<!-- Modal Produto -->
<div class="modal-backdrop" id="productModal">
  <div class="modal" role="dialog" aria-modal="true">
    <div class="content">
      <div style="flex:1">
        <img id="modalImg" src="" alt="" style="width:100%;height:360px;object-fit:cover;border-radius:8px">
      </div>
      <div style="flex:1">
        <h3 id="modalTitle">Título</h3>
        <p id="modalDesc" class="muted">Descrição</p>
        <div style="margin-top:12px"><strong id="modalPrice">R$ 0,00</strong></div>
        <div style="margin-top:18px;display:flex;gap:12px;align-items:center">
          <label>Quantidade</label>
          <input id="modalQty" type="number" value="1" min="1" style="width:70px;padding:8px;border-radius:8px;border:1px solid #eee">
        </div>
        <div style="margin-top:18px">
          <button id="addToCart" class="btn">Adicionar ao carrinho</button>
          <button id="closeModal" style="margin-left:8px;padding:10px 14px;border-radius:10px;border:1px solid #eee;background:transparent">Fechar</button>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Modal Carrinho -->
<div class="modal-backdrop" id="cartModal">
  <div class="modal">
    <div style="padding:18px">
      <h3>Seu carrinho</h3>
      <div id="cartItems" style="margin-top:12px"></div>
      <div style="display:flex;justify-content:space-between;align-items:center;margin-top:18px">
        <strong>Total: <span id="cartTotal">R$ 0,00</span></strong>
        <div>
          <button id="checkout" class="btn">Finalizar compra</button>
          <button id="closeCart" style="margin-left:8px;padding:10px 14px;border-radius:10px;border:1px solid #eee;background:transparent">Fechar</button>
        </div>
      </div>
    </div>
  </div>
</div>

  </main>  <footer>
    <div>
      <strong>Água & Sol</strong>
      <div class="muted">Moda praia • Feito com carinho</div>
    </div>
    <div class="muted">© <span id="year"></span> • Contato: contato@aguasolexample.com</div>
  </footer>  <script>
    // Dados de exemplo — troque pelas suas imagens e textos
    const products = [
      {id:1,title:'Biquíni Lenny',price:129.90,category:'bikini',img:'https://images.unsplash.com/photo-1519677100203-a0e668c92439?auto=format&fit=crop&w=800&q=60',desc:'Conjunto de duas peças em tecido leve e modelagem anatômica.'},
      {id:2,title:'Maiô Tropical',price:179.00,category:'maio',img:'https://images.unsplash.com/photo-1503342452485-86c6a39b58aa?auto=format&fit=crop&w=800&q=60',desc:'Maiô com bojo e proteção UV.'},
      {id:3,title:'Saída de Praia',price:89.90,category:'acessorio',img:'https://images.unsplash.com/photo-1520975915421-6f8a4e6bc0d8?auto=format&fit=crop&w=800&q=60',desc:'Saída leve e fresca para usar sobre o biquíni.'},
      {id:4,title:'Biquíni Coral',price:139.50,category:'bikini',img:'https://images.unsplash.com/photo-1487412947147-5cebf100ffc2?auto=format&fit=crop&w=800&q=60',desc:'Cortininha com tiras reguláveis.'},
      {id:5,title:'Chapéu Palha',price:69.00,category:'acessorio',img:'https://images.unsplash.com/photo-1495121605193-b116b5b09f3a?auto=format&fit=crop&w=800&q=60',desc:'Chapéu de palha com aba larga para proteção solar.'}
    ];

    const state = {cart:{}};

    const grid = document.querySelector('.grid');
    const searchInput = document.getElementById('searchInput');
    const filterCategory = document.getElementById('filterCategory');
    const sortBy = document.getElementById('sortBy');
    const productModal = document.getElementById('productModal');
    const cartModal = document.getElementById('cartModal');
    const cartCount = document.getElementById('cartCount');

    function formatPrice(n){return n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});}    

    function renderProducts(list){
      grid.innerHTML='';
      list.forEach(p=>{
        const card = document.createElement('article');card.className='card';
        card.innerHTML = `
          <img src="${p.img}" alt="${p.title}">
          <div class="info">
            <div style="display:flex;justify-content:space-between;align-items:center">
              <strong>${p.title}</strong>
              <div class="price">${formatPrice(p.price)}</div>
            </div>
            <div class="muted">${p.desc}</div>
            <div class="tags"><span class="tag">${p.category}</span></div>
            <div style="margin-top:10px;display:flex;gap:8px">
              <button data-id="${p.id}" class="openProduct">Ver</button>
              <button data-id="${p.id}" class="quickAdd">+ Carrinho</button>
            </div>
          </div>
        `;
        grid.appendChild(card);
      })
    }

    function applyFilters(){
      const q = searchInput.value.toLowerCase().trim();
      let list = products.filter(p=>{
        return (filterCategory.value==='all' || p.category===filterCategory.value)
          && (p.title.toLowerCase().includes(q) || p.desc.toLowerCase().includes(q));
      });
      if(sortBy.value==='price-asc') list.sort((a,b)=>a.price-b.price);
      if(sortBy.value==='price-desc') list.sort((a,b)=>b.price-a.price);
      renderProducts(list);
    }

    // Delegation para botões
    document.addEventListener('click', (e)=>{
      if(e.target.matches('.openProduct')){
        const id = +e.target.dataset.id; openProductModal(id);
      }
      if(e.target.matches('.quickAdd')){
        const id = +e.target.dataset.id; addToCart(id,1);
      }
      if(e.target.id==='closeModal' || e.target.id==='productModal'){
        productModal.style.display='none';
      }
      if(e.target.id==='openCart'){
        openCart();
      }
      if(e.target.id==='closeCart' || e.target.id==='cartModal'){
        cartModal.style.display='none';
      }
      if(e.target.id==='checkout'){
        alert('Simulação: encaminhar para checkout');
      }
    });

    function openProductModal(id){
      const p = products.find(x=>x.id===id);
      if(!p) return;
      document.getElementById('modalImg').src = p.img;
      document.getElementById('modalTitle').textContent = p.title;
      document.getElementById('modalDesc').textContent = p.desc;
      document.getElementById('modalPrice').textContent = formatPrice(p.price);
      document.getElementById('modalQty').value = 1;
      productModal.style.display='flex';

      document.getElementById('addToCart').onclick = ()=>{
        const qty = parseInt(document.getElementById('modalQty').value)||1;
        addToCart(p.id,qty);
        productModal.style.display='none';
      }
    }

    function addToCart(id,qty){
      state.cart[id] = (state.cart[id]||0) + qty;
      updateCartUI();
    }

    function updateCartUI(){
      const keys = Object.keys(state.cart);
      const count = keys.reduce((s,k)=>s+state.cart[k],0);
      cartCount.textContent = count;

      // Preencher modal
      const container = document.getElementById('cartItems');
      container.innerHTML='';
      let total = 0;
      keys.forEach(k=>{
        const p = products.find(x=>x.id==k);
        const qty = state.cart[k];
        const line = document.createElement('div');
        line.style.display='flex';line.style.justifyContent='space-between';line.style.alignItems='center';line.style.marginBottom='8px';
        line.innerHTML = `<div style="display:flex;gap:10px;align-items:center"><img src="${p.img}" style="width:48px;height:48px;object-fit:cover;border-radius:6px"><div><div>${p.title}</div><div class='muted' style='font-size:13px'>${formatPrice(p.price)} x ${qty}</div></div></div><div><button data-id='${p.id}' class='removeItem'>Remover</button></div>`;
        container.appendChild(line);
        total += p.price * qty;
      });
      document.getElementById('cartTotal').textContent = formatPrice(total);

      // remover item
      container.querySelectorAll('.removeItem').forEach(btn=>{
        btn.onclick = ()=>{
          const id = +btn.dataset.id; delete state.cart[id]; updateCartUI();
        }
      });
    }

    // Eventos de input
    searchInput.addEventListener('input', applyFilters);
    filterCategory.addEventListener('change', applyFilters);
    sortBy.addEventListener('change', applyFilters);

    document.getElementById('shopAll').addEventListener('click', ()=>{window.location.href='#produtos'});
    document.getElementById('exploreBtn').addEventListener('click', ()=>{window.location.href='#produtos'});

    // Inicialização
    applyFilters();
    updateCartUI();
    document.getElementById('year').textContent = new Date().getFullYear();
  </script></body>
</html>
